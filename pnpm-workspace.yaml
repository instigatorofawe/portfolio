allowBuilds:
  nx: true
# Allow nx's install script to run (pnpm blocks dependency build scripts by
# default). Settings live here in pnpm 10+; the `pnpm` field in package.json is
# no longer read.
onlyBuiltDependencies:
  - nx
