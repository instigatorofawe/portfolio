{
	"name": "portfolio",
	"private": true,
	"scripts": {
		"dev": "pnpm -C liuran dev",
		"build": "nx build liuran",
		"build:web": "pnpm -C liuran build",
		"build:wasm": "nx build-headsup-wasm pushfold",
		"build:wasm:threeway": "nx build-threeway-wasm pushfold",
		"lookups": "nx generate-lookups lookups",
		"lookups:threeway": "nx generate-threeway-lookups lookups",
		"clean:generated": "rm -rf pushfold/shared/src/generated/headsup liuran/src/lib/generated liuran/src/lib/pkg",
		"clean": "nx reset && pnpm clean:generated && rm -rf lookups/build pushfold/target liuran/.svelte-kit liuran/build",
		"rebuild": "pnpm clean && pnpm build",
		"ci:local": "pnpm build && pnpm check && pnpm lint && pnpm test:cpp && pnpm test:rust && pnpm -C liuran test:unit --run --passWithNoTests && pnpm test:e2e",
		"test:cpp": "cmake -S lookups -B lookups/build -DCMAKE_BUILD_TYPE=Release && cmake --build lookups/build --target pushfold_lookups_tests && ctest --test-dir lookups/build --output-on-failure",
		"test:rust": "cargo test --manifest-path pushfold/Cargo.toml",
		"preview": "pnpm -C liuran preview",
		"check": "pnpm -C liuran check",
		"lint": "pnpm -C liuran lint",
		"format": "pnpm -C liuran format",
		"test": "pnpm -C liuran test",
		"test:unit": "pnpm -C liuran test:unit",
		"test:e2e": "pnpm -C liuran test:e2e",
		"build:pushfold": "./scripts/build-pushfold.sh headsup"
	},
	"devDependencies": {
		"nx": "^23.0.0",
		"typescript": "^6.0.3"
	}
}
